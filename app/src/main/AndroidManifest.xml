<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.h5190004.derya_cayiroglu">

    <application
        android:allowBackup="true"
        android:name=".OyunApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_circle"
        android:supportsRtl="true"
        android:theme="@style/AppThemeFullScreen">
        <activity android:name=".activities.OyunSonuActivity"></activity>
        <activity android:name=".activities.YanlisCevapActivity" />
        <activity android:name=".activities.KategoriActivity" />
        <activity
            android:name=".activities.GirisActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SoruActivity" />
    </application>

</manifest>